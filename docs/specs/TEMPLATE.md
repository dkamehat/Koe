# Spec: <name>  (status: draft | ready | implemented)

> Written by the Architect. Must be verbatim-implementable: if the Implementer
> has to make a design decision, this spec has a bug. See docs/AGENTS.md.

## Goal
One paragraph: the user-visible outcome.

## Non-goals
What this deliberately does NOT do (and which DECISIONS.md entries apply).

## Context to read (exhaustive — Implementer reads nothing else)
- CLAUDE.md (always)
- <files this change touches or imitates>

## Design
Exact modules, classes, function signatures, constants (each with a WHY),
threading rules, config keys, CLI flags. Include code skeletons where the
shape matters.

## File-by-file changes
| File | Change |
|------|--------|

## Tests to write
Named tests with the behavior each one pins.

## Acceptance criteria
Numbered, mechanically checkable. The Implementer's self-check table must map
1:1 to these.

## Manual checks for the owner (things CI/agents cannot verify)
- ...

## Known pitfalls
- ...
